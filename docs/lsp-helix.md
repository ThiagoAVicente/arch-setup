# LSP

Some LSPs are already listed on `packages/helix`.

```bash
yay -S --needed $(cat packages/helix/yay.txt)
```

```bash
pacman -S --needed $(cat packages/helix/pacman.txt)
```

**Note:** `helix --health <language-x>` shows the lsps needed for **language-x**.
